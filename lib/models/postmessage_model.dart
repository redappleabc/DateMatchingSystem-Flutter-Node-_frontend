class PostMessageModel {
  final int id;
  final int senderId;
  final String name; 
  final int prefectureId;
  final int age; 
  final String content; 
  final String avatarImage;

  PostMessageModel(this.id, this.senderId, this.name, this.prefectureId, this.age, this.content, this.avatarImage);
}
